import {orders} from './trading.malloy'
import {sales_orders} from './sales_orders.malloy'

query: tot_orders_revenue is orders -> {
    aggregate:
        total_revenue
}
query: tot_amount is sales_orders -> {
    aggregate:
        total_amount
}
